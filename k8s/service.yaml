apiVersion: v1
kind: Service
metadata:
  name: signconnect-api
  namespace: backend
  labels:
    app: signconnect-api
spec:
  selector:
    app: signconnect-api
  ports:
  - port: 80
    targetPort: 4001
    protocol: TCP
    name: http
  type: ClusterIP